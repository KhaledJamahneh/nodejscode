# Disable analyzer to work around Dart VM crash
analyzer:
  enable-experiment:
    - no-analysis
  errors:
    # Treat all errors as warnings to prevent blocking
    invalid_assignment: warning
    undefined_identifier: warning
  exclude:
    - "**"
